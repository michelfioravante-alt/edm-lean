:root {
    --primary-color: #3b82f6; --accent-color: #f59e0b; --success-color: #10b981; 
    --danger-color: #ef4444; --warning-color: #f59e0b; --info-color: #06b6d4; 
    --bg-primary: #f9fafb; --bg-secondary: #ffffff; --border-color: #e5e7eb; 
    --text-primary: #111827; --text-secondary: #6b7280; --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1); 
    --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1); --radius-md: 8px; --radius-lg: 16px; 
    --classic-primary: #3498db; --classic-secondary: #2c3e50;
}
* { margin: 0; padding: 0; box-sizing: border-box; }
body { font-family: 'Inter', sans-serif; background-color: var(--bg-primary); color: var(--text-primary); line-height: 1.6; }

/* Estrutura do Header e Sidebar */
.header { background: linear-gradient(135deg, var(--classic-secondary) 0%, var(--classic-primary) 100%); color: white; padding: 15px 30px; position: sticky; top: 0; z-index: 90; box-shadow: var(--shadow-md); }
.header-content { display: flex; align-items: center; justify-content: space-between; max-width: 1800px; margin: 0 auto; }
.sidebar { position: fixed; top: 0; left: 0; width: 280px; height: 100vh; background-color: var(--classic-secondary); z-index: 110; transform: translateX(-100%); transition: transform 0.3s ease-in-out; display: flex; flex-direction: column; box-shadow: var(--shadow-lg); }
.sidebar.open { transform: translateX(0); }

/* Estilos do Kanban */
.kanban-board { display: grid; grid-template-columns: repeat(5, 1fr); gap: 16px; }
.kanban-column { background-color: var(--bg-primary); border-radius: var(--radius-md); padding: 10px; min-height: 60vh; }
.kanban-card { background-color: var(--bg-secondary); border: 1px solid var(--border-color); border-left: 5px solid var(--classic-primary); border-radius: var(--radius-md); padding: 16px; margin-top: 10px; cursor: grab; box-shadow: var(--shadow-md); position: relative; transition: all 0.2s ease; }
.kanban-card.is-paused { opacity: 0.6; }
.kanban-card.card-atrasado { border: 2px solid var(--danger-color); animation: pulse-red 2s infinite; }

/* Modais e Toasts */
.modal-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.5); z-index: 100; display: none; justify-content: center; align-items: center; }
.modal-overlay.active { display: flex; }
.modal-content { background: white; padding: 30px; border-radius: 15px; width: 90%; max-width: 800px; box-shadow: 0 10px 30px rgba(0,0,0,0.2); }

/* Utilitários de Gráficos e Tabelas */
.kpi-chart-container { background-color: #ffffff; padding: 24px; border-radius: var(--radius-md); box-shadow: var(--shadow-md); min-height: 520px; }
.table { width: 100%; border-collapse: collapse; margin-top: 20px; }
.status { padding: 4px 12px; border-radius: 20px; font-size: 0.75rem; font-weight: 700; text-transform: uppercase; }

/* Animações e Mobile */
@keyframes pulse-red { 0% { box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.7); } 70% { box-shadow: 0 0 0 10px rgba(239, 68, 68, 0); } }
@media (max-width: 768px) { .kanban-board { display: flex; overflow-x: auto; } }