/** Adiciona suporte local a um service — importe e chame no início de cada método. */
import { isLocalMode } from '../local/mode';
import { localApi } from '../local/localApi';
export { isLocalMode, localApi };
